// Käy läpi puolueiden ja vaalipiirien checkboxit ja päivitä graph datalla josta suodatettu ne mitä ei ole valittu
function refreshGraph() {
    //Käy läpi puolueet
    array.forEach(element => {
        
    });
    //Käy läpi vaalipiirit
    array.forEach(element => {
        
    });
    
    //päivitä graph
    //toteutus?
}